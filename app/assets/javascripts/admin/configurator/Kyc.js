;
(function (window ) {

  var oSTNs               = ns("ost"),
      formBuilder         = ns('ost.formBuilder'),
      oThis
  ;

  oSTNs.kycConfigurator  = oThis = {

    init: function ( config ) {
      formBuilder.init({});
    }

  };

})(window );