;
(function (window , $) {

  var oSTNs               = ns("ost"),
      formBuilder         = ns('ost.formBuilder'),
      oThis
  ;

  oSTNs.registerConfigurator  = oThis = {

    init: function ( config ) {
      formBuilder.init({});
    }

  };

})(window, jQuery );